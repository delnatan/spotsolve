# Python / Jupyter scratch
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store

# Third-party reference implementation, not this project's source
aguet_matlab/

# Papers and other binary reference material (the .md writeup IS tracked)
*.pdf

# Image data and run artifacts. The pipeline's inputs (beads_60x_still*.tif)
# and outputs (*.png/.log) are deliberately untracked: this repo is the source,
# not the dataset. Regenerate results with
#     python run.py --image beads_60x_still.tif --gain 4.23
*.tif
*.tiff
*.png
*.log
*.npz
*.pkl
*.mp4
*.txt
*.out
*.bak

# Notebooks: tracked source here is .py and .md only
*.ipynb
